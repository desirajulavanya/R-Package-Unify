# unfiy 0.0.0.9000

## Initial development version

- Added `audit_panel()` for panel structure diagnostics.
- Added `audit_summary()` for one-row audit summaries.
- Added `panel_gaps()` and `gap_summary()` for missing id-time diagnostics.
- Added `panel_duplicates()` and `duplicate_summary()` for duplicate id-time diagnostics.
- Added `flag_panel_issues()` for transparent row-level issue flags.
- Added `complete_panel()` for audited id-time completion without imputation.
